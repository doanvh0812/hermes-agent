# Worked transcript — 04/09/2026, BIDV slip 300.000 đ

## Input
Screenshot, Vietnamese banking app, "Chuyển tiền thành công".
- Số tiền `300,000 ₫` → 300000
- Ngày thực hiện `08:30 28/08/2026` → transfer_date `2026-08-28`
- Số giao dịch `6240VNIB02R3VIJ9`
- Ngân hàng `BIDV`
- Nội dung `Tran Luan chuyen khoan nhanh qua Zalo` — no class code in memo

Note the app renders the amount with a **comma** (`300,000 ₫`), unlike the
dot-separated `1.234.567` seen on other slips. Both mean the same thing; there
is no decimal part in VND.

## Missing data → asked
Memo carried no class code, so asked for: mã lớp, loại người nộp, tên học viên.
Reply: `Mã lớp A1-2026, học viên, Tran Luan`.

## Discovery of the model name
`pti.transfer.receipt` (guessed from the Agent.md wording) → blocked:

    Unreviewed side-effect methods are blocked by default. Review custom source,
    then add the exact 'model.method' to the policy file (ODOO_MCP_POLICY_FILE ...)

Resolved with:

    search_records(model="ir.model", domain=[["model","like","transfer"]], fields=["model","name"])
    → 1775 vni.transfer.receipt "Biên lai chuyển khoản"

`read_record("ir.model", 1775)` gives the docstring in `info`, which states the
receipt is *not* a payment and that turning it into one is a separate human
step (`action_reconcile`). Useful for wording the final reply.

## Matching
    find_billable_line(class_code="A1-2026", partner_name="Tran Luan")
    → {class_id: 1, class_name: "A1-2026", candidates: [
         {order_line_id: 1, order: "S00001", partner: "Tran Luan",
          product: "Khoa hoc Tieng Anh A1", price_unit: 900000.0,
          instalments_total: 8, instalments_invoiced: 3, instalments_remaining: 5}]}

Instalment number shown to the user = `instalments_invoiced + 1` = kỳ 4/8.

## Confirmation message sent (shape that worked)
Listed student, class, order, "đã xuất hóa đơn 3/8 kỳ, còn 5 kỳ — đây sẽ là kỳ
thứ 4", then the five slip figures, then: "Hiện chưa ghi nhận gì vào hệ thống.
Anh/chị trả lời 'đúng' thì em mới xuất hóa đơn kỳ 4 và ghi nhận thanh toán ạ."

User replied `ok` → sufficient agreement, same sender, same thread.

## The write
First attempt used kwargs → `TypeError: ... unexpected keyword argument
'order_line_id'`. Empty-kwargs probe → `missing 1 required positional argument:
'payload'`. Retried with `args=[{...}]`:

    {"success": true, "receipt_id": 5, "state": "reconciled",
     "move": "INV/2026/00006", "move_id": 6, "payment_id": 4,
     "amount_residual": 300000.0, "payment_state": "in_payment"}

`state: reconciled` on the receipt refers to the receipt record, **not** to bank
reconciliation. `payment_state: in_payment` and a non-zero `amount_residual`
are why the reply says "đang chờ đối soát với sao kê ngân hàng".

## Final reply
Invoice number, student + class + kỳ 4/8, amount, transaction ref, then the
awaiting-reconciliation line. No IDs, no model names.
